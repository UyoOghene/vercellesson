<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Glambox2</title>
</head>
<body>
    <h1>Home</h1>
    
    <% if (posts.length > 0) { %>
        <% for(let post of posts) { %>
            <h3><%= post.title %></h3> 
            <img src="<%= post.image %>" alt="Image" width="200">
            <p><%= post.caption %></p>
        <% } %>
    <% } else { %>
        <p>No posts found.</p>
    <% } %>

</body>
</html>
